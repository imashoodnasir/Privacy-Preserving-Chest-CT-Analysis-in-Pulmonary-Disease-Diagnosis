# Example configuration (optional). Scripts use CLI args; this file is for reference.
federated:
  rounds: 20
  clients_per_round: 3
  local_epochs: 1
  batch_size: 16
  lr: 1.0e-3
  weight_decay: 1.0e-4
  dp_sigma: 0.0
  dp_clip: 0.0
data:
  slice_stride: 1
  num_classes: 3
